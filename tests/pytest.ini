[pytest]
filterwarnings =
    ignore:Some syntactic constructs of Python 3*:UserWarning
    ignore:the imp module is deprecated*:DeprecationWarning

markers =
    slow: marks tests as slow (deselect with '-m "not slow"')
    serial

junit_family=xunit1
